#include <windows.h>

DWORD x32 = reintepeter_cast<void*> buf();
DWORD x86_64 = reintepeter_cast<void*> FuncLoad();

void tokeneliminate()
{
    enum token_load {
        TokenSys,
        ReLoadSys
    };
};
