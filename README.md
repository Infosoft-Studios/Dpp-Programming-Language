# D++ Programming-Language
A new Language has arrived and created by Infosoft Corporation. **D++ programming language has the following syntax**. Before we continue, the D++ programming language is a Programming language with having a rust style syntax, of course, different rules and Systems, But, yes we know, the repo name is D-Programming-Language, we did a mistake, its a typo, anyways here is an example, before that it has following features:

- Windows Core integration(RE, PE or bootmgr)
- OS dev(Bootloaders, kernel and UI* via system.graphics.design libary)
- Windows Kernel load(via Win32 if you are using: system.windows.win32.api)
- Hardware control
- OOP
- Accesses(via memory/class pointers)
- Windows bootloader
 and many more ...

# Code samples
Here is a code sample:

Kernel distribution in VM:

```dpp
using system.windows.winv.ver.wmsdos.win32.api;
using system.config.win.win32.api.server;
using system.windows.msdos;

fn::load -> created(Main): protected{
  kernel::loaddistrubution: krnl.load;
  end::line* protected
}
```

# Copyright
if you have a .dpp or .dppt, then please note that it will have the IE(internet explorer) Logo, because the problem is that we have the .reg file that get =s the logo **from your computer in this case from C:\\Program Files (x86)\\Internet Explorer\\ieinstal.exe**, so it is being used from your computer, *NOTE*: Your computer has not been harmed, in anyway.

# Disclaimer
Use the Software cautious

Alrighty then, good luck
Current version; 1.0
